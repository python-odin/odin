#########
CSV Codec
#########

Codec for serialising and de-serialising sequences of resources into CSV format.

The CSV CODEC differs from many other CODECS in that data can be streamed, or read
using an iterator.

.. automodule:: odin.codecs.csv_codec
    :members:
