language: python
python:
  - "2.7"
  - "3.3"
install: "pip install -r requirements.txt --use-mirrors && pip install -r requirements-optional.txt --use-mirrors"
script: nosetests --all-modules --with-coverage --cover-package odin tests/
